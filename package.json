{
  "name": "stilla-themes",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": ["packages/*"],
  "scripts": {
    "generate": "bun run --cwd packages/colors build",
    "build": "bun run generate && bun run --cwd packages/vscode build"
  }
}
