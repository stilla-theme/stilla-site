The site install section in `packages/site/index.html` now serves as the canonical place for discovery + downloads. It includes direct links to VS Code Marketplace, Open VSX, raw downloadable theme files (VS Code JSON, Neovim Vim/Lua files, Doom Emacs `.el`), and GitHub package source pages. If TODOs about marketplace links or downloadable theme formats come up again, check this section first.